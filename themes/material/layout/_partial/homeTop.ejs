<div class="homeServicesShort">
<!-- services short -->
<% site.posts.forEach(function(item){ %>
    <%  if(item.type=="services") { %>
            <div class="service">
                    <img src="<%- item.icon %>" border="0" class="serviceIcon" /><br>
                    <h2><%- item.title %></h2>
                <%- markdown(item.text) %>
            </div>
    <%  } %>
<% }); %>
</div>


        <!-- services short -->
        <% alter=0; site.posts.forEach(function(item){ %>
            <%  if(item.type=="services") { %>
            <div class="homeServicesLong">
                <div class="servicesLongImage <% if(alter<=0) { %>servicesLongImageLeft<% alter=1; } else { %>servicesLongImageRight<% alter=0; };  %>" style="background-image:url(<%- item.image %>);"></div>
                <div class="servicesLong">
                    <h1><img src="<%- item.icon %>" border="0" class="serviceIcon" /><%- item.title %></h1>
                        <%- markdown(item.textlong) %>
                </div>
            </div>
            <%  } %>
        <% }); %>


